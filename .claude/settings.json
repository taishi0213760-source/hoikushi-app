{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "export PATH=\"/tmp/node-v22.14.0-darwin-arm64/bin:$PATH\" && cd /Users/satoutaishi/Desktop/hoikushi-app && npx tsc --noEmit 2>&1 | head -20"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(echo '$CLAUDE_TOOL_INPUT' | python3 -c \"import sys,json; d=json.load(sys.stdin); print(d.get('file_path',''))\" 2>/dev/null); if echo \"$file\" | grep -qE 'questions\\.ts$'; then echo '[GUARD] questions.ts を編集します。Question/Choice 型の構造を壊さないよう注意してください。'; fi"
          }
        ]
      }
    ]
  }
}
